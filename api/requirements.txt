# Portfolio API Dependencies - Lightweight Production Build

# Core FastAPI dependencies
fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic[email]>=2.7.0
pydantic-settings>=2.4.0
python-multipart>=0.0.18
python-dotenv==1.0.0
httpx>=0.27.0
pyyaml==6.0.1

# Essential dependencies only - CPU optimized
numpy>=1.21.0,<2.0

# LLM Providers
anthropic>=0.39.0  # Claude API (primary)

# HTTP Client for Ollama
requests>=2.31.0   # Required for Ollama embeddings API

# ChromaDB for vector storage (must match server version 0.5.18)
chromadb==0.5.18
urllib3>=2.6.0 # not directly required, pinned by Snyk to avoid a vulnerability
